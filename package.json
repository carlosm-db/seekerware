{
  "name": "seekerware",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "migrate:local": "wrangler d1 migrations apply seekerware --local",
    "migrate:remote": "wrangler d1 migrations apply seekerware --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.112.0"
  },
  "dependencies": {
    "hono": "^4.12.30"
  }
}
